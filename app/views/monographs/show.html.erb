<p id="notice"><%= notice %></p>

<div class='container'>

  <div class='row'>
    <div class='col-sm-6 col-sm-offset-3'>
      <h3><%= @monograph.title %></h3>
    </div>
  </div>

  <br>
  <div class='col-sm-6 col-sm-offset-3'>
    <%= @monograph.body %>
  </div>
</div>

<%= link_to 'Edit', edit_monograph_path(@monograph) %> |
<%= link_to 'Back', monographs_path %>
