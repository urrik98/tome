<div class="container">
  <div class="row">
    <div class="col-md-12">
      <p>
        <% @topics.each do |t| %>
          <%= link_to t.name, topic_path(t.id), :class => "btn btn-xs btn-tag" %>&nbsp;
        <% end %>
      </p>
    </div>
  </div>
</div>
