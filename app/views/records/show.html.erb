<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1">
      <h5><%= @record.title %></h5>
      <p><%= @record.body %></p>
      <h6><% @record.technology.each do |t| %><%= t %>, <% end %></h6>
    </div>
  </div>
</div>


<%= link_to 'Edit', edit_record_path(@record) %> |
<%= link_to 'Back', records_path %>
